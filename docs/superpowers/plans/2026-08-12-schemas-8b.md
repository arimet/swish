# Schémas 8B : l'animation — Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Jouer une combinaison : les pions glissent d'un temps au suivant, le ballon suit les passes, et un lecteur plein écran sert au temps-mort.

**Architecture:** Un domaine pur (`src/domain/anim.ts`) qui répond « où est chaque chose à l'instant t » en rendant un `Temps` — donc directement consommable par le `PlayBoard` de 8A, sans toucher au rendu. Un écran lecteur hors coquille par-dessus. Aucun champ ajouté au modèle, aucune migration Dexie.

**Tech Stack:** React 19, TypeScript, Vite, Vitest + Testing Library, Tailwind v4. Aucune dépendance ajoutée.

## Global Constraints

- Commentaires, libellés et messages de commit **en français accentué** (`é è à ç`). Le dépôt entier est ainsi et le lint l'accepte. Si des accents disparaissent d'un fichier écrit, réécrire le fichier entier avec Write puis vérifier avec `grep -c 'à\|é' <fichier>`.
- **La lecture n'est jamais protégée** : le lecteur ne demande aucun code, quel que soit le rôle.
- Coordonnées **normalisées 0..1** dans le terrain choisi ; sur `complet`, la moitié avant est y ≤ 0,5.
- **Le modèle de 8A n'est pas touché** : aucun champ ajouté à `Schema`, `Temps` ou `Fleche`, aucune version Dexie. Si une tâche semble l'exiger, c'est que la conception est à revoir — signale-le plutôt que d'ajouter un champ.
- Aucune dépendance npm ajoutée.
- Commandes : `pnpm test`, `pnpm lint`, `pnpm build` — les trois passent avant chaque commit.

---

### Task 1 : Le recalage d'un tracé

**Files:**
- Create: `src/domain/anim.ts`, `src/domain/anim.test.ts`

**Interfaces:**
- Consumes: `Point` de `src/domain/plays.ts`.
- Produces: `recaler(points: Point[], depart: Point, arrivee: Point): Point[]`.

Une flèche a été dessinée à la main ; ses extrémités ne coïncident pas avec les positions des temps. `recaler` applique au tracé la **similitude** — rotation, échelle uniforme, translation — qui envoie `points[0]` sur `depart` et `points[dernier]` sur `arrivee`. La forme du geste est préservée, les extrémités deviennent exactes.

- [ ] **Step 1: Écrire les tests qui échouent**

```ts
import { describe, expect, it } from 'vitest'
import { recaler } from './anim'

const proche = (a: { x: number; y: number }, b: { x: number; y: number }) => {
  expect(a.x).toBeCloseTo(b.x, 6); expect(a.y).toBeCloseTo(b.y, 6)
}

describe('recaler', () => {
  it('fait tomber les extrémités exactement sur le départ et l’arrivée', () => {
    const trace = [{ x: 0.2, y: 0.2 }, { x: 0.2, y: 0.6 }, { x: 0.6, y: 0.6 }]
    const r = recaler(trace, { x: 0.1, y: 0.1 }, { x: 0.9, y: 0.5 })
    proche(r[0], { x: 0.1, y: 0.1 })
    proche(r[r.length - 1], { x: 0.9, y: 0.5 })
  })

  it('préserve la forme : un L reste un L', () => {
    // Le coude est à angle droit ; une similitude conserve les angles.
    const trace = [{ x: 0, y: 0 }, { x: 0, y: 1 }, { x: 1, y: 1 }]
    const r = recaler(trace, { x: 0.2, y: 0.3 }, { x: 0.8, y: 0.7 })
    const u = { x: r[0].x - r[1].x, y: r[0].y - r[1].y }
    const v = { x: r[2].x - r[1].x, y: r[2].y - r[1].y }
    expect(u.x * v.x + u.y * v.y).toBeCloseTo(0, 6)   // produit scalaire nul
  })

  it('conserve les proportions le long du tracé', () => {
    //点 milieu d’un segment droit reste au milieu après recalage.
    const trace = [{ x: 0, y: 0 }, { x: 0.5, y: 0 }, { x: 1, y: 0 }]
    const r = recaler(trace, { x: 0.2, y: 0.2 }, { x: 0.8, y: 0.8 })
    proche(r[1], { x: 0.5, y: 0.5 })
  })

  it('retombe sur la ligne droite quand le tracé est dégénéré', () => {
    // Extrémités confondues : aucune similitude n’est définie.
    const trace = [{ x: 0.3, y: 0.3 }, { x: 0.4, y: 0.5 }, { x: 0.3, y: 0.3 }]
    const r = recaler(trace, { x: 0.1, y: 0.1 }, { x: 0.9, y: 0.9 })
    expect(r).toEqual([{ x: 0.1, y: 0.1 }, { x: 0.9, y: 0.9 }])
  })

  it('laisse tel quel un tracé de moins de deux points, ramené aux bornes', () => {
    expect(recaler([], { x: 0.1, y: 0.1 }, { x: 0.9, y: 0.9 })).toEqual([{ x: 0.1, y: 0.1 }, { x: 0.9, y: 0.9 }])
  })
})
```

**Attention** : le troisième test contient un caractère non latin dans un commentaire (`点`) — **remplace-le par « Le »** en écrivant le fichier. C'est un test de vigilance : recopie le code, pas les coquilles.

- [ ] **Step 2: Lancer et vérifier l'échec**

Run: `pnpm vitest run src/domain/anim.test.ts`
Expected: FAIL — `Failed to resolve import "./anim"`.

- [ ] **Step 3: Implémenter**

La similitude qui envoie `a → depart` et `b → arrivee`, où `a = points[0]`, `b = points[dernier]` :

```ts
export function recaler(points: Point[], depart: Point, arrivee: Point): Point[] {
  if (points.length < 2) return [depart, arrivee]
  const a = points[0], b = points[points.length - 1]
  const dx = b.x - a.x, dy = b.y - a.y
  const n = dx * dx + dy * dy
  if (n === 0) return [depart, arrivee]
  const ex = arrivee.x - depart.x, ey = arrivee.y - depart.y
  // Nombre complexe (k + i·r) = (arrivee - depart) / (b - a) : rotation + échelle.
  const k = (ex * dx + ey * dy) / n
  const r = (ey * dx - ex * dy) / n
  return points.map((p) => {
    const ux = p.x - a.x, uy = p.y - a.y
    return { x: depart.x + k * ux - r * uy, y: depart.y + r * ux + k * uy }
  })
}
```

- [ ] **Step 4: Vérifier**

Run: `pnpm vitest run src/domain/anim.test.ts` puis `pnpm test && pnpm lint && pnpm build`

- [ ] **Step 5: Vérifier par mutation**

Remplace la similitude par une simple translation (`k = 1, r = 0`) : le test des extrémités doit tomber. Restaure.

- [ ] **Step 6: Commit**

```bash
git add src/domain/anim.ts src/domain/anim.test.ts
git commit -m "feat(schémas) : recaler un tracé dessiné sur ses positions réelles"
```

---

### Task 2 : L'instantané

**Files:**
- Modify: `src/domain/anim.ts`, `src/domain/anim.test.ts`

**Interfaces:**
- Consumes: `recaler` ; `Schema`, `Temps`, `Pion`, `Point` de `plays.ts`.
- Produces: `interface Instant { temps: number; part: number }`, `instantane(s: Schema, at: Instant): Temps`, `transitions(s: Schema): number`.

- [ ] **Step 1: Écrire les tests qui échouent**

```ts
import { instantane, transitions } from './anim'
import { nouveauSchema, tempsSuivant, type Schema } from './plays'

/** Deux temps : le poste 1 descend de (0.5,0.62) à (0.5,0.2), les autres ne bougent pas. */
function deuxTemps(): Schema {
  const s: Schema = { id: 'x', ...nouveauSchema('c1', 'demi', false) }
  const t1 = tempsSuivant(s.temps[0])
  t1.pions = t1.pions.map((p) => (p.poste === 1 ? { ...p, at: { x: 0.5, y: 0.2 } } : p))
  s.temps = [s.temps[0], t1]
  return s
}

describe('instantane', () => {
  it('rend exactement le temps de départ à part 0, et le suivant à part 1', () => {
    const s = deuxTemps()
    expect(instantane(s, { temps: 0, part: 0 }).pions).toEqual(s.temps[0].pions)
    expect(instantane(s, { temps: 0, part: 1 }).pions).toEqual(s.temps[1].pions)
  })

  it('interpole en ligne droite le pion sans flèche', () => {
    const s = deuxTemps()
    const p = instantane(s, { temps: 0, part: 0.5 }).pions.find((q) => q.poste === 1)!
    expect(p.at.x).toBeCloseTo(0.5, 6)
    expect(p.at.y).toBeCloseTo(0.41, 6)          // (0.62 + 0.2) / 2
  })

  it('laisse strictement immobile un pion qui ne bouge pas', () => {
    const s = deuxTemps()
    const avant = s.temps[0].pions.find((q) => q.poste === 3)!
    const p = instantane(s, { temps: 0, part: 0.37 }).pions.find((q) => q.poste === 3)!
    expect(p.at).toEqual(avant.at)
  })

  it('suit la forme de la flèche plutôt que la corde', () => {
    // Une course qui contourne par la gauche : à mi-chemin, le pion est à l’écart
    // de la ligne droite qui joint les deux positions.
    const s = deuxTemps()
    s.temps[0] = { ...s.temps[0], fleches: [{
      depuis: { camp: 'attaque', poste: 1 },
      points: [{ x: 0.5, y: 0.62 }, { x: 0.2, y: 0.41 }, { x: 0.5, y: 0.2 }],
      trait: 'course',
    }] }
    const p = instantane(s, { temps: 0, part: 0.5 }).pions.find((q) => q.poste === 1)!
    expect(p.at.x).toBeLessThan(0.4)             // nettement à gauche de la corde
  })

  it('n’expose aucune flèche : l’animation montre les joueurs, pas les traits', () => {
    const s = deuxTemps()
    s.temps[0] = { ...s.temps[0], fleches: [{
      depuis: { camp: 'attaque', poste: 1 }, points: [{ x: 0.5, y: 0.62 }, { x: 0.5, y: 0.2 }], trait: 'course',
    }] }
    expect(instantane(s, { temps: 0, part: 0.5 }).fleches).toEqual([])
  })

  it('mène le ballon au nouveau porteur quand il change de mains', () => {
    const s = deuxTemps()
    s.temps[1] = { ...s.temps[1], ballon: { camp: 'attaque', poste: 3 } }
    const mi = instantane(s, { temps: 0, part: 0.5 })
    const depart = s.temps[0].pions.find((q) => q.poste === 1)!.at
    const arrivee = s.temps[1].pions.find((q) => q.poste === 3)!.at
    // Le ballon est en vol : posé au sol, à mi-distance des deux porteurs.
    expect('x' in mi.ballon).toBe(true)
    const b = mi.ballon as { x: number; y: number }
    expect(b.x).toBeCloseTo((depart.x + arrivee.x) / 2, 6)
  })

  it('garde le ballon porté quand le porteur ne change pas', () => {
    const s = deuxTemps()
    expect(instantane(s, { temps: 0, part: 0.5 }).ballon).toEqual({ camp: 'attaque', poste: 1 })
  })

  it('compte les transitions, et rend le dernier temps au-delà', () => {
    const s = deuxTemps()
    expect(transitions(s)).toBe(1)
    expect(instantane(s, { temps: 5, part: 0.5 }).pions).toEqual(s.temps[1].pions)
  })
})
```

- [ ] **Step 2: Lancer et vérifier l'échec**

- [ ] **Step 3: Implémenter**

`transitions(s) = Math.max(0, s.temps.length - 1)`.

`instantane(s, at)` :
- Borne `temps` à `[0, transitions(s)]` ; au-delà du dernier, rendre le dernier temps tel quel (flèches vidées).
- Pour chaque pion du temps N : chercher son homologue `(camp, poste)` au temps N+1. Absent (la défense vient d'être ajoutée) : le laisser immobile. Positions égales : le laisser strictement immobile, sans interpoler.
- S'il existe une flèche de ce pion au temps N dont le `trait` est `course`, `ecran` ou `dribble` : `recaler(f.points, depart, arrivee)` puis avancer de `part` **le long du tracé, à vitesse constante** (paramétrer par la longueur cumulée, pas par le rang des points — sinon la vitesse dépend de l'échantillonnage). Sinon : interpolation linéaire.
- Ballon : porté au temps N et au temps N+1 par le même pion → reste porté. Porteur différent, ou passage porté ↔ posé → il est **en vol** pendant la transition : rendre un `Point`. Le trajet suit une flèche `passe` partant du porteur si elle existe (recalée sur les positions réelles), sinon la ligne droite. À `part = 0` et `part = 1`, rendre exactement la valeur du temps correspondant, pour que les deux premiers tests tiennent.
- `fleches: []` toujours.
- `pions` : conserver l'ordre du temps N, pour que `toEqual` soit stable.

Un utilitaire local `avanceSur(points, part): Point` (position à la fraction `part` de la longueur totale) sert aux pions comme au ballon.

- [ ] **Step 4: Vérifier**

Run: `pnpm test && pnpm lint && pnpm build`

- [ ] **Step 5: Vérifier par mutation**

Deux mutations : paramétrer le trajet par le rang des points au lieu de la longueur cumulée (le test de la forme doit rester vert, mais vérifie qu'un test de vitesse constante — ajoute-le si tu n'en as pas — tombe) ; et faire suivre la flèche brute sans `recaler` (le test « part 1 rend le temps suivant » doit tomber). Restaure après chaque.

- [ ] **Step 6: Commit**

```bash
git add -A && git commit -m "feat(schémas) : l'instantané — où est chaque chose à l'instant t"
```

---

### Task 3 : Le lecteur plein écran

**Files:**
- Create: `src/ui/screens/SchemaPlayer.tsx`, `src/ui/screens/SchemaPlayer.test.tsx`
- Modify: `src/App.tsx` (route `/schemas/:id/lecteur`, **hors** de `OliveShell`, comme `/match/:id/watch`), `src/ui/screens/SchemaView.tsx` et `src/ui/screens/SchemaList.tsx` (entrées vers le lecteur)

**Interfaces:**
- Consumes: `instantane`, `transitions`, `getPlay`, `PlayBoard`.
- Produces: l'écran du temps-mort.

- [ ] **Step 1: Écrire les tests qui échouent**

Les cas, à écrire complètement sur le modèle des tests d'écran du dépôt :

- avancer et reculer d'un temps par les deux zones tactiles, **bornés** aux extrémités (boutons éteints, l'affichage ne change pas) ;
- « Lecture » démarre, « Pause » arrête, et le tableau reste à l'endroit atteint ;
- la boucle repart du premier temps une fois le dernier atteint ;
- le ralenti double la durée d'une transition ;
- `prefers-reduced-motion` : la lecture saute de temps en temps, sans interpolation ;
- un visiteur (aucun rôle) ouvre le lecteur et fait défiler sans qu'aucun code lui soit demandé.

**Deux pièges jsdom à traiter, sinon les tests mentent :**

`requestAnimationFrame` n'avance pas tout seul. Utilise les minuteurs simulés de Vitest et une horloge injectable, ou remplace `requestAnimationFrame` par un pilote de test. Le plus simple et le plus robuste : que le composant lise le temps par `performance.now()` et que le test avance `vi.useFakeTimers()` ; commande la boucle par `setTimeout` plutôt que par `requestAnimationFrame` si cela rend le test honnête — **une boucle testable vaut mieux qu'une boucle élégante non testée**, et à 1,5 s par transition la différence est invisible à l'œil.

`matchMedia` n'existe pas en jsdom : le stubber dans `beforeEach` pour le test de `prefers-reduced-motion`.

- [ ] **Step 2: Lancer et vérifier l'échec**

- [ ] **Step 3: Implémenter**

État : `instant: Instant`, `enLecture: boolean`, `boucle: boolean`, `ralenti: boolean`.

Rendu : `<PlayBoard schema={schema} tempsIndex={0} />` ne convient pas — il faut rendre un `Temps` calculé. **Ajoute à `PlayBoard` une prop optionnelle `temps?: Temps`** qui, si elle est fournie, remplace `schema.temps[tempsIndex]`. C'est un ajout rétrocompatible d'une ligne, pas une modification du modèle.

Mise en page : hors coquille, fond sombre, terrain au maximum (mêmes bornes de largeur que les autres écrans : `maxWidth: '46vh'` sur demi-terrain, pleine largeur sur complet). Nom du schéma discret en haut, bouton de sortie visible (retour vers `/schemas/:id`). Deux zones tactiles moitié-écran superposées au terrain pour reculer/avancer. En bas : Lecture/Pause, Boucle, Ralenti, et une barre d'avancement `<input type="range">` sur `[0, transitions]` avec un pas fin, qui se déplace pendant la lecture et qu'on peut saisir pour se placer.

Durée d'une transition : `1500 ms`, `3000 ms` au ralenti. Pause quand l'onglet passe en arrière-plan (`document.visibilitychange`). Sous `prefers-reduced-motion`, `part` ne prend que 0 et 1.

- [ ] **Step 4: Vérifier**

Run: `pnpm test && pnpm lint && pnpm build`

- [ ] **Step 5: Vérifier le rendu réel**

L'aperçu tourne (outil d'aperçu du harnais, port 5280 ; jamais Bash). Ouvre le lecteur du pick and roll : la combinaison se joue-t-elle de façon lisible ? L'écran arrive-t-il au bon moment ? Le ballon part-il du passeur pour arriver au receveur ? Vérifie aussi le terrain complet et la boucle. Rapporte ce que tu as vu, pas ce que tu supposes.

- [ ] **Step 6: Commit**

```bash
git add -A && git commit -m "feat(schémas) : le lecteur du temps-mort — lecture, boucle, ralenti"
```

---

## Auto-relecture

**Couverture du spec**

| Section du spec | Tâche |
|---|---|
| §2 Recalage d'un tracé | Task 1 |
| §1 Domaine de l'animation | Task 2 |
| §3 Lecteur plein écran | Task 3 |
| §4 Cadence, arrière-plan, `prefers-reduced-motion` | Task 3 |
| §5 Le modèle n'est pas touché | contrainte globale, vérifiée à chaque tâche |

**Points d'attention pour l'exécutant**
- La flèche est un dessin, pas une trajectoire : on interpole **entre les positions des temps**, et la flèche ne sert qu'à courber le trajet. Suivre la flèche brute ferait arriver le pion à côté de sa position réelle.
- Paramétrer un trajet par le rang des points fait varier la vitesse avec l'échantillonnage : c'est la longueur cumulée qui compte.
- Une boucle d'animation non testable ne vaut rien : préfère la forme que le test peut piloter.
- Aucun champ ajouté au modèle de 8A. La seule modification permise hors du sous-projet est la prop `temps` de `PlayBoard`.
