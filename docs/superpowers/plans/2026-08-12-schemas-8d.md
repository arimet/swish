# Schémas 8D : sortie et partage — Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Sortir une combinaison de l'application : un lien qui la porte entièrement, une image, un PDF, un GIF animé.

**Architecture:** Un domaine pur pour le codage (`src/domain/partage.ts`), qui compresse le schéma avec `CompressionStream` — présent dans tous les navigateurs cibles, donc aucune dépendance. Un écran de réception qui réutilise le lecteur de 8B. Un dialogue de partage qui sérialise le SVG déjà rendu.

**Tech Stack:** React 19, TypeScript, Vite, Vitest + Testing Library, Tailwind v4. Aucune dépendance ajoutée.

## Global Constraints

- Commentaires, libellés et messages de commit **en français accentué** (`é è à ç`). Le dépôt entier est ainsi et le lint l'accepte. Si des accents disparaissent d'un fichier écrit, réécrire le fichier entier avec Write puis vérifier avec `grep -c 'à\|é' <fichier>`.
- **Partager et exporter sont libres** : aucun code demandé, quel que soit le rôle. Seul « Ajouter à ma bibliothèque » passe par `guard('manage', …)`.
- **Aucune dépendance npm ajoutée.** Ni bibliothèque de compression, ni générateur de PDF, ni encodeur GIF. Si une tâche semble l'exiger, c'est que la conception est à revoir — signale-le plutôt que d'installer quoi que ce soit.
- Le schéma voyage dans le **fragment** de l'URL (`#`), jamais dans la requête.
- Aucune version Dexie ajoutée.
- Commandes : `pnpm test`, `pnpm lint`, `pnpm build` — les trois passent avant chaque commit.

---

### Task 1 : Le codage d'un schéma dans une URL

**Files:**
- Create: `src/domain/partage.ts`, `src/domain/partage.test.ts`

**Interfaces:**
- Consumes: `Schema` de `src/domain/plays.ts`.
- Produces: `encoder(s: Schema): Promise<string>`, `decoder(code: string): Promise<Schema | null>`, `LIMITE_LIEN = 8000`.

- [ ] **Step 1: Écrire les tests qui échouent**

```ts
import { describe, expect, it } from 'vitest'
import { encoder, decoder, LIMITE_LIEN } from './partage'
import { nouveauSchema, tempsSuivant, type Schema } from './plays'

const complet = (): Schema => {
  const s: Schema = { id: 'x1', ...nouveauSchema('club-a', 'complet', true), nom: 'Presse tout terrain', note: 'Sortie de balle' }
  const t0 = { ...s.temps[0], fleches: [{ depuis: { camp: 'attaque' as const, poste: 1 as const }, trait: 'dribble' as const, points: [{ x: 0.5, y: 0.4 }, { x: 0.3, y: 0.25 }] }] }
  return { ...s, temps: [t0, tempsSuivant(t0)], objets: [{ sorte: 'plot', at: { x: 0.2, y: 0.3 } }], dossier: 'Presse', majLe: '2026-08-01T10:00:00.000Z' }
}

describe('encoder / decoder', () => {
  it('rend un schéma équivalent après un aller-retour', async () => {
    const s = complet()
    const recu = await decoder(await encoder(s))
    expect(recu).not.toBeNull()
    expect(recu!.nom).toBe(s.nom)
    expect(recu!.note).toBe(s.note)
    expect(recu!.terrain).toBe(s.terrain)
    expect(recu!.defense).toBe(s.defense)
    expect(recu!.temps).toEqual(s.temps)
    expect(recu!.objets).toEqual(s.objets)
  })

  it('retire ce qui n’a pas de sens ailleurs', async () => {
    const recu = (await decoder(await encoder(complet())))!
    expect(recu.id).toBe('')
    expect(recu.clubId).toBe('')
    expect(recu.majLe).toBeUndefined()
    expect(recu.dossier).toBeUndefined()
  })

  it('produit un code sans caractère à échapper dans une URL', async () => {
    const code = await encoder(complet())
    expect(code).toMatch(/^[A-Za-z0-9_-]+$/)
    expect(encodeURIComponent(code)).toBe(code)
  })

  it('reste très en deçà de la limite pour un schéma ordinaire', async () => {
    expect((await encoder(complet())).length).toBeLessThan(LIMITE_LIEN / 4)
  })

  it('rend null sur un code vide, tronqué, ou qui n’est pas un schéma', async () => {
    expect(await decoder('')).toBeNull()
    expect(await decoder('pas-du-tout-un-code')).toBeNull()
    const code = await encoder(complet())
    expect(await decoder(code.slice(0, Math.floor(code.length / 2)))).toBeNull()
  })

  it('rend null quand le contenu décompressé n’a pas la forme d’un schéma', async () => {
    // Un JSON valide mais qui n'est pas un schéma : la validation porte sur la
    // forme, pas seulement sur la décompression.
    const octets = new TextEncoder().encode(JSON.stringify({ bonjour: 'monde' }))
    const flux = new Blob([octets]).stream().pipeThrough(new CompressionStream('deflate-raw'))
    const brut = new Uint8Array(await new Response(flux).arrayBuffer())
    const b64 = btoa(String.fromCharCode(...brut)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '')
    expect(await decoder(b64)).toBeNull()
  })
})
```

- [ ] **Step 2: Lancer et vérifier l'échec**

Run: `pnpm vitest run src/domain/partage.test.ts`
Expected: FAIL — `Failed to resolve import "./partage"`.

**Si `CompressionStream` n'existe pas dans l'environnement de test**, ne l'abandonne pas et n'installe rien : Node 18+ l'expose globalement. Vérifie la version de Node du projet avant de conclure quoi que ce soit.

- [ ] **Step 3: Implémenter**

`encoder` : construire l'objet à transmettre — `nom`, `note`, `terrain`, `defense`, `objets`, `temps` — le sérialiser en JSON, le compresser par `CompressionStream('deflate-raw')`, encoder en base64url (`+`→`-`, `/`→`_`, sans `=`).

`decoder` : l'inverse, puis **valider la forme** avant de rendre quoi que ce soit — `terrain` vaut `demi` ou `complet`, `temps` est un tableau non vide dont chaque élément a `pions`, `ballon` et `fleches`, `objets` est un tableau. Toute anomalie, toute exception de décompression : `null`. Les champs absents du transport reviennent à leur valeur neutre : `id: ''`, `clubId: ''`, `majLe` et `dossier` absents.

- [ ] **Step 4: Vérifier**

Run: `pnpm test && pnpm lint && pnpm build`

- [ ] **Step 5: Vérifier par mutation**

Retire la validation de forme de `decoder` : le test « pas la forme d'un schéma » doit tomber. Restaure.

- [ ] **Step 6: Commit**

```bash
git add src/domain/partage.ts src/domain/partage.test.ts
git commit -m "feat(schémas) : un schéma qui tient dans une URL"
```

---

### Task 2 : L'écran de réception

**Files:**
- Create: `src/ui/screens/SchemaRecu.tsx`, `src/ui/screens/SchemaRecu.test.tsx`
- Modify: `src/App.tsx` (route `/schemas/recu`, **hors** de la coquille et **hors** du garde de club, comme `/match/:id/watch`)

**Interfaces:**
- Consumes: `decoder`, `PlayBoard`, `instantane`/`transitions` de `src/domain/anim.ts`, `savePlay`, `newId`, `useAuth`, `useClub`.

- [ ] **Step 1: Écrire les tests qui échouent**

Les cas, à écrire complètement toi-même sur le modèle de `SchemaPlayer.test.tsx` :

- un lien valide affiche le nom du schéma reçu et son tableau ;
- **il est jouable** : les temps défilent ;
- un lien abîmé affiche « Ce lien est incomplet ou abîmé », pas une page blanche ;
- « Ajouter à ma bibliothèque » crée un schéma **neuf** : identifiant différent de zéro, `clubId` du club courant, et l'original n'est pas touché ;
- l'ajout est refusé à la table de marque, et rien n'est écrit ;
- **aucun code n'est demandé pour lire** le schéma reçu, quel que soit le rôle.

Le fragment se pose dans le test via `MemoryRouter initialEntries={['/schemas/recu#' + code]}` — vérifie comment `react-router` expose le fragment (`useLocation().hash`) avant d'écrire l'écran.

- [ ] **Step 2: Lancer et vérifier l'échec**

- [ ] **Step 3: Implémenter**

L'écran lit `useLocation().hash` (sans le `#`), appelle `decoder`, et rend trois états : en cours de décodage, abîmé, ou le schéma. Réutilise la présentation du lecteur — terrain en grand, défilement des temps — sans dupliquer sa logique d'animation : ici, le défilement à la main suffit, et un lien vers le lecteur complet n'aurait pas de schéma en base à viser.

« Ajouter à ma bibliothèque » : `guard('manage', …)` puis `savePlay({ ...recu, id: newId(), clubId })`, et navigation vers la fiche du schéma créé. Sans club réglé, afficher l'invitation à choisir un club plutôt qu'un bouton qui échouerait.

- [ ] **Step 4: Vérifier**

Run: `pnpm test && pnpm lint && pnpm build`

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat(schémas) : recevoir une combinaison par un lien"
```

---

### Task 3 : Le partage et les sorties fichier

**Files:**
- Create: `src/ui/components/ExportSchema.tsx`, `src/ui/components/ExportSchema.test.tsx`
- Modify: `src/ui/screens/SchemaView.tsx`, `src/ui/screens/SchemaPlayer.tsx` (bouton Partager)

- [ ] **Step 1: Écrire les tests qui échouent**

Ce qui se teste honnêtement en jsdom, et ce qui ne se teste pas : le canvas n'y rend rien, `navigator.share` n'existe pas. **N'écris pas de test qui prétend vérifier le contenu d'un PNG** — il ne prouverait rien.

Ce qui se teste :
- le dialogue propose les quatre sorties ;
- le lien produit contient le fragment codé, et rouvrir ce lien rend le schéma d'origine (aller-retour au niveau de l'écran, en réutilisant `decoder`) ;
- au-delà de `LIMITE_LIEN`, le lien n'est pas proposé et un message explique pourquoi ;
- `navigator.share` est appelé quand il existe (doublure), et l'on retombe sur un téléchargement sinon ;
- aucun code n'est demandé pour partager, quel que soit le rôle.

- [ ] **Step 2: Lancer et vérifier l'échec**

- [ ] **Step 3: Implémenter**

Le dialogue (`ConfirmDialog` n'est pas adapté : fais un `Dialog` du dépôt, cf. `src/app/auth.tsx` pour le patron) propose : **Lien**, **Image PNG**, **PDF**, **GIF animé**.

**Lien** : `${location.origin}/schemas/recu#${code}`. Copie dans le presse-papiers, et `navigator.share` s'il existe.

**PNG** : sérialiser le SVG rendu (`XMLSerializer`), le charger dans une `Image` via un blob, le dessiner dans un `canvas` à deux fois la taille du viewBox, sur fond opaque (`C.frame`), puis `toBlob`.

**PDF** : une page par temps. Écris l'octet-à-octet toi-même — en-tête `%PDF-1.4`, un objet image par page, une table de références croisées. C'est une centaine de lignes et cela évite une dépendance. **Si tu juges après tentative que c'est déraisonnable dans le temps imparti, dis-le franchement et livre les trois autres sorties** : mieux vaut trois sorties solides qu'un PDF fragile. Ne réponds pas à cette difficulté en installant une bibliothèque.

**GIF** : composer les instantanés de 8B à dix images par seconde. Même règle que pour le PDF : c'est la sortie la plus coûteuse, elle est la première à sacrifier si le reste doit en souffrir. Dis-le franchement plutôt que de livrer quelque chose qui ne marche qu'à moitié.

- [ ] **Step 4: Vérifier au rendu réel**

L'aperçu tourne (outil d'aperçu du harnais, port 5280 ; jamais Bash). **C'est ici que tout se joue** : produis un lien, ouvre-le dans un nouvel onglet, vérifie que le schéma arrive entier et se joue. Exporte un PNG et **regarde-le**. Rapporte ce que tu as vu, y compris ce qui est laid.

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat(schémas) : partager par lien, image, PDF ou GIF"
```

---

## Auto-relecture

**Couverture du spec**

| Section du spec | Tâche |
|---|---|
| §1 Codage, validation, limite | Task 1 |
| §2 Écran de réception | Task 2 |
| §3 Sorties fichier | Task 3 |
| §4 Où l'on partage | Task 3 |
| §5 Droits | Tasks 2 et 3 (tests dédiés) |
| §6 Ce que le partage dit de ses limites | Task 3 |

**Points d'attention pour l'exécutant**
- Aucune dépendance npm, quelle que soit la difficulté rencontrée. La bonne réponse à un blocage est de le dire, pas de l'installer.
- Le schéma va dans le fragment, jamais dans la requête.
- Un schéma reçu est un schéma neuf : sans identifiant ni club, il ne peut écraser personne.
- Ne teste pas ce que jsdom ne sait pas rendre. Un test qui ne peut pas tomber est pire que pas de test.
