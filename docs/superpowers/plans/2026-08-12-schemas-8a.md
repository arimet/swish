# Schémas 8A : le tableau tactique — Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Un tableau tactique : demi-terrain ou terrain complet, cinq attaquants, défense optionnelle, ballon, trajectoires tracées au doigt, temps successifs.

**Architecture:** Un domaine pur (`src/domain/plays.ts`) qui porte tout ce qui se teste sans écran ; une table Dexie v5 locale à l'appareil ; un composant de rendu (`PlayBoard`) séparé de la carte des tirs ; deux écrans (bibliothèque, éditeur) sous les droits du projet 7.

**Tech Stack:** React 19, TypeScript, Vite, Dexie, Vitest + Testing Library, Tailwind v4. Aucune dépendance ajoutée.

## Global Constraints

- Commentaires, libellés et messages de commit **en français accentué** (`é è à ç`). Le dépôt entier est ainsi et le lint l'accepte. Si des accents disparaissent d'un fichier écrit, réécrire le fichier entier avec Write puis vérifier avec `grep -c 'à\|é' <fichier>`.
- **La lecture n'est jamais protégée** : bibliothèque et consultation d'un schéma sans code, quel que soit le rôle. Créer, modifier, supprimer : `guard('manage', …)`.
- Coordonnées **normalisées 0..1** dans le terrain choisi. Sur `demi`, y va de la ligne de fond (0) à la ligne médiane (1) ; sur `complet`, de la ligne de fond haute (0) à la ligne de fond basse (1), la médiane à 0,5. **La moitié avant est y ≤ 0,5.**
- Les schémas **ne passent pas par la file de synchronisation** (pas de `enqueuePut`/`enqueueDel`), comme les résultats. Les écrans le disent : « Ces schémas restent sur cet appareil : ils ne sont pas synchronisés avec vos autres appareils. »
- Aucune dépendance npm ajoutée.
- Commandes : `pnpm test`, `pnpm lint`, `pnpm build` — les trois passent avant chaque commit.

---

### Task 1 : Le domaine des schémas

**Files:**
- Create: `src/domain/plays.ts`, `src/domain/plays.test.ts`

**Interfaces:**
- Produces: les types `Terrain`, `Camp`, `Poste`, `Trait`, `Point`, `Pion`, `Fleche`, `Temps`, `ObjetPose`, `Schema` et les fonctions `nouveauSchema(clubId, terrain, defense): Schema` (sans `id` : `Omit<Schema,'id'>`), `tempsSuivant(t: Temps): Temps`, `reduireTrace(points: Point[], epsilon?: number): Point[]`, `versTerrain(s: Schema, terrain: Terrain): { ok: Schema } | { refus: string }`, `PANIER: Record<Terrain, Point[]>`.

- [ ] **Step 1: Écrire les tests qui échouent**

Créer `src/domain/plays.test.ts`. Les cas, avec leurs assertions réelles :

```ts
import { describe, expect, it } from 'vitest'
import { nouveauSchema, tempsSuivant, reduireTrace, versTerrain, type Schema } from './plays'

describe('nouveauSchema', () => {
  it('pose un 1-2-2 de cinq attaquants, ballon au meneur, sur demi-terrain', () => {
    const s = nouveauSchema('c1', 'demi', false)
    expect(s.temps).toHaveLength(1)
    expect(s.temps[0].pions).toHaveLength(5)
    expect(s.temps[0].pions.every((p) => p.camp === 'attaque')).toBe(true)
    expect(s.temps[0].ballon).toEqual({ camp: 'attaque', poste: 1 })
    // 1-2-2 : le meneur est le plus loin du panier (y le plus grand, ligne de fond en haut)
    const meneur = s.temps[0].pions.find((p) => p.poste === 1)!
    expect(Math.max(...s.temps[0].pions.map((p) => p.at.y))).toBe(meneur.at.y)
  })

  it('ajoute cinq défenseurs, chacun entre son attaquant et le panier', () => {
    const s = nouveauSchema('c1', 'demi', true)
    expect(s.temps[0].pions).toHaveLength(10)
    const att1 = s.temps[0].pions.find((p) => p.camp === 'attaque' && p.poste === 1)!
    const def1 = s.temps[0].pions.find((p) => p.camp === 'defense' && p.poste === 1)!
    // Plus proche du panier (0.5, 0.1125) que son attaquant
    const d = (pt: { x: number; y: number }) => Math.hypot(pt.x - 0.5, pt.y - 0.1125)
    expect(d(def1.at)).toBeLessThan(d(att1.at))
  })

  it('occupe la moitié avant sur terrain complet', () => {
    const s = nouveauSchema('c1', 'complet', true)
    expect(s.temps[0].pions.every((p) => p.at.y <= 0.5)).toBe(true)
  })
})

describe('tempsSuivant', () => {
  it('hérite des positions et du ballon, jamais des flèches', () => {
    const s = nouveauSchema('c1', 'demi', false)
    const t0 = { ...s.temps[0], fleches: [{ depuis: { camp: 'attaque' as const, poste: 1 as const }, points: [{ x: 0.5, y: 0.7 }, { x: 0.4, y: 0.3 }], trait: 'course' as const }] }
    const t1 = tempsSuivant(t0)
    expect(t1.pions).toEqual(t0.pions)
    expect(t1.pions).not.toBe(t0.pions)          // copie, pas la même référence
    expect(t1.ballon).toEqual(t0.ballon)
    expect(t1.fleches).toEqual([])
  })
})

describe('reduireTrace', () => {
  it('garde le coude d’un tracé en L', () => {
    // Un L : descente verticale puis départ horizontal, échantillonné en 41 points
    const pts = [
      ...Array.from({ length: 21 }, (_, i) => ({ x: 0.2, y: 0.2 + i * 0.02 })),
      ...Array.from({ length: 20 }, (_, i) => ({ x: 0.2 + (i + 1) * 0.02, y: 0.6 })),
    ]
    const r = reduireTrace(pts)
    expect(r.length).toBeLessThanOrEqual(5)
    // Le coude (0.2, 0.6) survit à la réduction
    expect(r.some((p) => Math.hypot(p.x - 0.2, p.y - 0.6) < 0.01)).toBe(true)
    // Les extrémités sont conservées exactement
    expect(r[0]).toEqual(pts[0]); expect(r[r.length - 1]).toEqual(pts[pts.length - 1])
  })

  it('réduit fortement un geste bruité sans le déformer', () => {
    const pts = Array.from({ length: 200 }, (_, i) => ({ x: 0.1 + i * 0.004, y: 0.5 + Math.sin(i / 8) * 0.001 }))
    const r = reduireTrace(pts)
    expect(r.length).toBeLessThanOrEqual(10)
  })

  it('laisse tel quel un tracé de moins de trois points', () => {
    const pts = [{ x: 0.1, y: 0.1 }, { x: 0.9, y: 0.9 }]
    expect(reduireTrace(pts)).toEqual(pts)
  })
})

describe('versTerrain', () => {
  it('remappe demi → complet dans la moitié avant, sans perte', () => {
    const s: Schema = { id: 'x', ...nouveauSchema('c1', 'demi', false) }
    const r = versTerrain(s, 'complet')
    if ('refus' in r) throw new Error(r.refus)
    expect(r.ok.terrain).toBe('complet')
    expect(r.ok.temps[0].pions.every((p) => p.at.y <= 0.5)).toBe(true)
  })

  it('refuse complet → demi quand la moitié arrière est occupée, en nommant l’occupant', () => {
    const s: Schema = { id: 'x', ...nouveauSchema('c1', 'complet', false) }
    s.temps[0].pions[2].at = { x: 0.5, y: 0.8 }   // le poste 3 dans la moitié arrière
    const r = versTerrain(s, 'demi')
    expect('refus' in r && /3/.test(r.refus)).toBe(true)
  })

  it('accepte complet → demi quand tout tient dans la moitié avant', () => {
    const s: Schema = { id: 'x', ...nouveauSchema('c1', 'complet', false) }
    const r = versTerrain(s, 'demi')
    if ('refus' in r) throw new Error(r.refus)
    expect(r.ok.terrain).toBe('demi')
    // aller-retour : on retombe (à l’arrondi près) sur le 1-2-2 du demi-terrain
    const attendu = nouveauSchema('c1', 'demi', false)
    r.ok.temps[0].pions.forEach((p, i) => {
      expect(p.at.x).toBeCloseTo(attendu.temps[0].pions[i].at.x, 6)
      expect(p.at.y).toBeCloseTo(attendu.temps[0].pions[i].at.y, 6)
    })
  })

  it('même terrain : rend le schéma inchangé', () => {
    const s: Schema = { id: 'x', ...nouveauSchema('c1', 'demi', false) }
    const r = versTerrain(s, 'demi')
    expect('ok' in r && r.ok).toEqual(s)
  })
})
```

- [ ] **Step 2: Lancer et vérifier l'échec**

Run: `pnpm vitest run src/domain/plays.test.ts`
Expected: FAIL — `Failed to resolve import "./plays"`.

- [ ] **Step 3: Écrire le domaine**

Créer `src/domain/plays.ts`. Les types du spec, puis :

```ts
/** Position du panier, normalisée, par terrain (1,575 m de la ligne de fond). */
export const PANIER: Record<Terrain, Point[]> = {
  demi: [{ x: 0.5, y: 1.575 / 14 }],
  complet: [{ x: 0.5, y: 1.575 / 28 }, { x: 0.5, y: 1 - 1.575 / 28 }],
}

// 1-2-2 sur demi-terrain : meneur en tête de raquette, deux ailiers, deux postes bas.
const MISE_EN_PLACE: Record<Poste, Point> = {
  1: { x: 0.5, y: 0.62 }, 2: { x: 0.22, y: 0.48 }, 3: { x: 0.78, y: 0.48 },
  4: { x: 0.3, y: 0.2 }, 5: { x: 0.7, y: 0.2 },
}
```

`nouveauSchema(clubId, terrain, defense)` : les cinq attaquants aux positions ci-dessus (y divisé par 2 sur `complet`) ; si `defense`, cinq défenseurs chacun **au milieu du segment attaquant-panier** (panier avant : `PANIER[terrain][0]`) ; ballon `{ camp: 'attaque', poste: 1 }` ; `objets: []` ; un seul temps ; pas d'`id` (c'est la persistance qui le donne).

`tempsSuivant(t)` : copie **profonde** des pions et du ballon, `fleches: []`.

`reduireTrace(points, epsilon = 0.01)` : Ramer-Douglas-Peucker classique — récursif, distance perpendiculaire au segment extrémités, seuil en unités normalisées. Moins de trois points : retour tel quel.

`versTerrain(s, terrain)` : même terrain → `{ ok: s }`. `demi → complet` : tous les `y` divisés par 2 (pions, points de flèches, objets, ballon posé). `complet → demi` : parcourir pions, flèches, objets, ballon posé ; au premier `y > 0.5`, refus avec un message nommant l'occupant (« le poste 3 », « une flèche du poste 2 », « un plot », « le ballon ») ; sinon tous les `y` multipliés par 2.

- [ ] **Step 4: Vérifier**

Run: `pnpm vitest run src/domain/plays.test.ts` puis `pnpm test && pnpm lint && pnpm build`
Expected: tout vert.

- [ ] **Step 5: Vérifier les tests par mutation**

Deux mutations, chacune : casser, constater l'échec du test visé, restaurer.
- Dans `tempsSuivant`, copier aussi les flèches → le test « jamais des flèches » tombe.
- Dans `versTerrain`, remapper complet → demi sans le contrôle d'occupation → le test du refus tombe.

- [ ] **Step 6: Commit**

```bash
git add src/domain/plays.ts src/domain/plays.test.ts
git commit -m "feat(schémas) : le domaine du tableau tactique — temps, tracés, terrains"
```

---

### Task 2 : La persistance

**Files:**
- Modify: `src/persistence/db.ts` (version 5), `src/persistence/repositories.ts`
- Test: `src/persistence/repositories.test.ts` (étendre l'existant)

**Interfaces:**
- Consumes: `Schema` de `src/domain/plays.ts`.
- Produces: `listPlays(clubId: string): Promise<Schema[]>`, `getPlay(id: string): Promise<Schema | undefined>`, `savePlay(s: Schema): Promise<void>`, `deletePlay(id: string): Promise<void>` ; `deleteTeam` cascade sur les schémas du club.

- [ ] **Step 1: Écrire les tests qui échouent**

Dans `repositories.test.ts`, sur le modèle des tests existants des résultats :

```ts
it('enregistre, liste par club et supprime un schéma', async () => {
  await savePlay({ id: 's1', ...nouveauSchema('ta', 'demi', false), nom: 'PnR haut' })
  await savePlay({ id: 's2', ...nouveauSchema('tb', 'demi', false), nom: 'Autre club' })
  expect((await listPlays('ta')).map((s) => s.id)).toEqual(['s1'])
  await deletePlay('s1')
  expect(await listPlays('ta')).toEqual([])
})

it('supprimer une équipe emporte ses schémas', async () => {
  await saveTeam({ id: 'ta', name: 'VIGNOT' })
  await savePlay({ id: 's1', ...nouveauSchema('ta', 'demi', false), nom: 'PnR haut' })
  await deleteTeam('ta')
  expect(await listPlays('ta')).toEqual([])
})

it('les schémas ne passent pas par la file de synchronisation', async () => {
  await savePlay({ id: 's1', ...nouveauSchema('ta', 'demi', false), nom: 'PnR haut' })
  await deletePlay('s1')
  expect(await db.outbox.count()).toBe(0)
})
```

Adapter le nom exact de la table de file (`outbox`) à ce que `db.ts` déclare — le lire d'abord.

- [ ] **Step 2: Lancer et vérifier l'échec**

- [ ] **Step 3: Implémenter**

`db.ts` : `this.version(5).stores({ plays: 'id, clubId' })` — **strictement additif**, ne pas toucher aux versions 1 à 4. `repositories.ts` : CRUD direct sur `db.plays`, sans `enqueuePut`/`enqueueDel` ; dans `deleteTeam`, ajouter la suppression `db.plays.where('clubId').equals(id).delete()` dans la même transaction que les cascades existantes.

- [ ] **Step 4: Vérifier**

Run: `pnpm test && pnpm lint && pnpm build`

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat(schémas) : table Dexie v5, dépôt local et cascade d'équipe"
```

---

### Task 3 : Le rendu du tableau — `PlayBoard`

**Files:**
- Create: `src/ui/components/PlayBoard.tsx`, `src/ui/components/PlayBoard.test.tsx`

**Interfaces:**
- Consumes: les types de `src/domain/plays.ts` ; la géométrie de `ShotCourt.tsx` (lire son `HalfCourtLines` et ses constantes W=1500, D=1400 — les tracés sont à réexporter ou dupliquer proprement, voir Step 3).
- Produces: `PlayBoard({ schema, tempsIndex, onPointerDown?, onPointerMove?, onPointerUp?, children?, apercu? })` — rend le terrain (demi : viewBox 1500×1400 ; complet : 1500×2800 avec la seconde moitié en miroir), les objets, puis le temps demandé : flèches (course pleine, écran à barre en T, passe pointillée, dribble ondulé), pions (disques numérotés pour l'attaque, croix ×N pour la défense), ballon (point orange sur le porteur, ou au sol). `apercu` : rendu minimal pour vignettes (pas de gestion du pointeur). Exporte aussi `versSvg(e: PointerEvent|MouseEvent, svg: SVGSVGElement): Point` — la conversion écran → coordonnées normalisées, utilisée par l'éditeur.

- [ ] **Step 1: Écrire les tests qui échouent**

```tsx
import { render, screen } from '@testing-library/react'
import { describe, expect, it } from 'vitest'
import { PlayBoard } from './PlayBoard'
import { nouveauSchema } from '../../domain/plays'

const demi = { id: 'x', ...nouveauSchema('c1', 'demi', true) }

describe('PlayBoard', () => {
  it('rend les dix pions et le ballon du temps demandé', () => {
    render(<PlayBoard schema={demi} tempsIndex={0} />)
    // Cinq attaquants numérotés, cinq croix
    ;[1, 2, 3, 4, 5].forEach((n) => expect(screen.getByText(String(n))).toBeInTheDocument())
    ;[1, 2, 3, 4, 5].forEach((n) => expect(screen.getByText(`×${n}`)).toBeInTheDocument())
    expect(screen.getByLabelText('ballon')).toBeInTheDocument()
  })

  it('distingue les quatre traits de flèche', () => {
    const s = { ...demi }
    s.temps = [{ ...s.temps[0], fleches: (['course', 'ecran', 'passe', 'dribble'] as const).map((trait, i) => ({
      depuis: { camp: 'attaque' as const, poste: (i + 1) as 1|2|3|4|5 }, points: [{ x: 0.1 + i * 0.2, y: 0.7 }, { x: 0.1 + i * 0.2, y: 0.3 }], trait,
    })) }]
    const { container } = render(<PlayBoard schema={s} tempsIndex={0} />)
    expect(container.querySelectorAll('[data-trait="course"]')).toHaveLength(1)
    expect(container.querySelectorAll('[data-trait="ecran"]')).toHaveLength(1)
    expect(container.querySelectorAll('[data-trait="passe"]')).toHaveLength(1)
    expect(container.querySelectorAll('[data-trait="dribble"]')).toHaveLength(1)
  })

  it('double la profondeur du viewBox sur terrain complet', () => {
    const s = { id: 'y', ...nouveauSchema('c1', 'complet', false) }
    const { container } = render(<PlayBoard schema={s} tempsIndex={0} />)
    expect(container.querySelector('svg')!.getAttribute('viewBox')).toBe('0 0 1500 2800')
  })
})
```

- [ ] **Step 2: Lancer et vérifier l'échec**

- [ ] **Step 3: Implémenter**

Le tracé du demi-terrain vit aujourd'hui dans un composant interne de `ShotCourt.tsx` (`HalfCourtLines`). **L'exporter** depuis `ShotCourt.tsx` (export nommé, aucun changement de comportement) et le consommer dans `PlayBoard` — pas de duplication des paths. Terrain complet : `<HalfCourtLines/>` puis `<g transform="translate(0 2800) scale(1 -1)"><HalfCourtLines/></g>`.

Rendu d'un temps : les flèches d'abord (sous les pions). Chaque flèche est un `<path>` lissé (Catmull-Rom → Bézier, ou lignes droites si 2 points) portant `data-trait` ; `passe` : `strokeDasharray` ; `dribble` : le chemin est redessiné en petites ondulations (déplacement perpendiculaire alterné le long du tracé) ; `ecran` : barre en T perpendiculaire au dernier segment à la place de la pointe ; les autres : pointe de flèche (deux segments) au bout. Pions : `<g>` avec disque plein numéroté (attaque, couleur `C.accent`) ou croix `×N` (défense, `C.muted`). Ballon : disque orange de rayon ~28 unités, `aria-label="ballon"`, posé sur le pion porteur ou aux coordonnées du sol.

`versSvg` : conversion via `getBoundingClientRect` du SVG, bornée 0..1 (`clamp01` existe dans `ShotCourt.tsx`).

- [ ] **Step 4: Vérifier**

Run: `pnpm test && pnpm lint && pnpm build`

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat(schémas) : le tableau — deux terrains, pions, traits du carnet"
```

---

### Task 4 : L'éditeur

**Files:**
- Create: `src/ui/screens/SchemaEdit.tsx`, `src/ui/screens/SchemaEdit.test.tsx`
- Modify: `src/App.tsx` (route `/schemas/:id/edit` dans la coquille)

**Interfaces:**
- Consumes: `PlayBoard`, `versSvg`, le domaine, `getPlay`/`savePlay`, `useAuth` (`guard('manage', …)`), `useClub`.
- Produces: l'écran d'édition complet. Enregistrement automatique : chaque geste abouti écrit en base (pas de bouton « Enregistrer »).

- [ ] **Step 1: Écrire les tests qui échouent**

Piège jsdom à poser en tête de fichier : `getBoundingClientRect` rend des zéros, la conversion écran → SVG diviserait par zéro. Le neutraliser :

```tsx
beforeEach(() => {
  Element.prototype.getBoundingClientRect = function () {
    return { x: 0, y: 0, left: 0, top: 0, right: 300, bottom: 280, width: 300, height: 280, toJSON: () => ({}) } as DOMRect
  }
})
```

Les cas (rôle admin posé par `sessionStorage.setItem(ROLE_KEY, 'admin')`, schéma de départ écrit en base par `savePlay`) :

```tsx
it('trace une flèche de course depuis un pion et l’enregistre', async () => {
  renderEdit('s1')
  await userEvent.click(await screen.findByRole('button', { name: 'Course' }))
  const svg = screen.getByLabelText(/tableau tactique/i)
  // du meneur (0.5, 0.62) → (150, 173.6) px vers le corner (30, 250)
  fireEvent.pointerDown(svg, { clientX: 150, clientY: 174 })
  fireEvent.pointerMove(svg, { clientX: 90, clientY: 220 })
  fireEvent.pointerUp(svg, { clientX: 30, clientY: 250 })
  await waitFor(async () => expect((await getPlay('s1'))!.temps[0].fleches).toHaveLength(1))
  expect((await getPlay('s1'))!.temps[0].fleches[0].trait).toBe('course')
  expect((await getPlay('s1'))!.temps[0].fleches[0].depuis).toEqual({ camp: 'attaque', poste: 1 })
})

it('la gomme retire une flèche ; annuler la restaure', async () => { /* pointerDown sur la flèche en mode Gomme, assertion base à 0, clic « Annuler », assertion base à 1 */ })

it('« + » ajoute un temps qui hérite des positions, pas des flèches', async () => { /* tracer, cliquer +, vérifier temps[1].pions == temps[0].pions et fleches == [] */ })

it('le passage complet → demi est refusé si la moitié arrière est occupée', async () => { /* schéma complet avec un pion à y 0.8, choisir « Demi-terrain », le message nomme l'occupant, le terrain reste complet */ })

it('la table de marque ne peut pas modifier : le tracé demande le code admin', async () => { /* ROLE_KEY marque, tracer, heading Accès Administrateur requis, base inchangée */ })
```

Un tracé dont `pointerDown` ne tombe pas sur un pion ne fait rien (à tester aussi : aucune flèche écrite).

- [ ] **Step 2: Lancer et vérifier l'échec**

- [ ] **Step 3: Implémenter**

État local : `schema` (source de vérité éditée), `tempsIndex`, `outil` (`'deplacer' | Trait | 'ballon' | 'objet' | 'gomme'`), `sorteObjet`, `trace: Point[] | null` (le geste en cours), `pileAnnulation: Temps[][]` (une pile par temps, plafonnée à 20).

Gestes sur le SVG (Pointer Events, `setPointerCapture`) :
- `deplacer` : `pointerDown` sur un pion/objet le saisit (rayon de prise ~0,05 normalisé), `pointerMove` le suit, `pointerUp` pousse l'état précédent sur la pile puis `savePlay`.
- trait : `pointerDown` doit toucher un pion (sinon rien) ; `pointerMove` accumule `trace` (affichée en cours de geste) ; `pointerUp` : `reduireTrace`, ajout de la flèche, pile, `savePlay`.
- `ballon` : tap sur un pion → porté ; tap ailleurs → posé à ce point.
- `objet` : tap → pose de `sorteObjet`.
- `gomme` : tap → supprime la flèche la plus proche (distance au tracé < 0,04) ou l'objet sous le doigt.

**Chaque écriture passe par `guard('manage', () => { … savePlay(...) })`** — l'état local n'est muté que dans l'action gardée, jamais avant (la convention du projet 7 : garder d'abord, muter ensuite).

Bande des temps : vignettes `PlayBoard apercu`, sélection, « + » (`tempsSuivant`), ◀ ▶ pour réordonner, suppression refusée sur le dernier temps restant. En-tête : nom (input), note, boutons Demi/Complet (via `versTerrain`, le refus s'affiche sous les boutons), interrupteur Défense (désactivation confirmée par `ConfirmDialog` : retire les croix et leurs flèches).

La phrase locale : « Ces schémas restent sur cet appareil : ils ne sont pas synchronisés avec vos autres appareils. » en pied d'écran.

- [ ] **Step 4: Vérifier, mutation comprise**

`pnpm test && pnpm lint && pnpm build`. Mutation : dans le gestionnaire de tracé, écrire la flèche sans `reduireTrace` → aucun test ne doit tomber sur la longueur exacte (on ne fige pas l'algorithme), mais muter `tempsSuivant` en copiant les flèches → le test « + » tombe. Restaurer.

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat(schémas) : l'éditeur — glisser, tracer, gommer, annuler, temps"
```

---

### Task 5 : La bibliothèque, les routes, le menu

**Files:**
- Create: `src/ui/screens/SchemaList.tsx`, `src/ui/screens/SchemaList.test.tsx`, `src/ui/screens/SchemaView.tsx`, `src/ui/screens/SchemaView.test.tsx`
- Modify: `src/App.tsx` (routes `/schemas`, `/schemas/:id`), `src/ui/olive/OliveShell.tsx` (entrée « Schémas » dans `NAV_REST` et `TITLES`)

**Interfaces:**
- Consumes: `listPlays`, `savePlay`, `deletePlay`, `nouveauSchema`, `PlayBoard apercu`, `guard`.
- Produces: la bibliothèque (vignettes du premier temps, créer/dupliquer/supprimer sous `manage`) et la consultation (`SchemaView` : le tableau en grand, boutons ◀ ▶ entre les temps, bornés au premier et au dernier — pas de boucle —, lien « Modifier » sous `manage`).

- [ ] **Step 1: Écrire les tests qui échouent**

```tsx
it('liste les schémas du club en vignettes', async () => { /* deux savePlay, les deux noms visibles, un svg par vignette */ })
it('créer un schéma est administratif', async () => { /* ROLE_KEY marque, clic « Nouveau schéma », heading Accès Administrateur requis, base vide */ })
it('un visiteur consulte un schéma sans code', async () => { /* sans rôle, SchemaView rend le tableau, aucune demande */ })
it('le défilement des temps se borne aux extrémités', async () => { /* schéma à 2 temps : ◀ inerte au temps 1, ▶ inerte au temps 2 */ })
it('supprimer un schéma est confirmé puis effectif', async () => { /* admin, suppression via ConfirmDialog, base vide */ })
```

- [ ] **Step 2: Lancer et vérifier l'échec**

- [ ] **Step 3: Implémenter**

`SchemaList` : grille de cartes — vignette `PlayBoard apercu` du premier temps, nom, badge du terrain, nombre de temps ; « Nouveau schéma » (`guard('manage', …)` puis navigation vers l'éditeur) ; dupliquer et supprimer sous `manage` ; la phrase « sur cet appareil ». `SchemaView` : consultation libre, `PlayBoard` en grand, « Temps n / N », ◀ ▶ bornés, « Modifier » qui passe par `guard`. Menu : entrée « Schémas » (icône existante du kit), titre dans `TITLES`.

- [ ] **Step 4: Vérifier**

Run: `pnpm test && pnpm lint && pnpm build`

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat(schémas) : bibliothèque, consultation et entrée de menu"
```

---

### Task 6 : La démonstration et la vérification au rendu réel

**Files:**
- Modify: `src/dev/seed.ts` (`SEED_VERSION` **incrémenté** — l'oublier rend tout invisible, c'est arrivé au projet 3)
- Test: `src/dev/seed.test.ts` si présent, sinon assertion dans `repositories.test.ts`

- [ ] **Step 1: Étendre le seed**

Trois schémas pour le club de démonstration, construits avec le domaine (pas de JSON figé) :
1. « Pick and roll haut » — demi, défense, 3 temps : écran du 5 sur le porteur, course du 1, passe au 5 qui roule.
2. « Corner pour le 4 » — demi, sans défense, 2 temps.
3. « Remise ligne de fond » — complet, sans défense, 2 temps, ballon **posé** au premier temps puis porté.

- [ ] **Step 2: Test**

```ts
it('la démo contient trois schémas, dont un sur terrain complet et un ballon posé', async () => { /* après seed : 3, un terrain complet, un temps[0].ballon qui est un Point */ })
```

- [ ] **Step 3: Vérifier au rendu réel**

L'aperçu tourne déjà (outil d'aperçu du harnais, port 5280). Vider IndexedDB si le seed ne se recharge pas (piège connu). Contrôler : la bibliothèque montre trois vignettes reconnaissables ; l'éditeur ouvre le pick and roll, un glissé de pion suit le doigt, une course se trace et s'affiche lissée ; le passage complet → demi de la remise en jeu est refusé avec son message ; la table de marque se voit refuser un tracé ; un visiteur consulte tout sans code. Rapporter ce qui a été vu.

- [ ] **Step 4: Vérifier**

Run: `pnpm test && pnpm lint && pnpm build`

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat(schémas) : trois combinaisons de démonstration"
```

---

## Auto-relecture

**Couverture du spec**

| Section du spec | Tâche |
|---|---|
| §1 Modèle et fonctions du domaine | Task 1 |
| §2 Persistance, v5, cascade, pas de synchro | Task 2 |
| §3 Éditeur (rendu : Task 3 ; gestes : Task 4) | Tasks 3–4 |
| §4 Bibliothèque et consultation | Task 5 |
| §5 Droits | Tasks 4–5 (tests dédiés) |
| §6 Démonstration | Task 6 |

**Points d'attention pour l'exécutant**
- Le piège `getBoundingClientRect` en jsdom (Task 4, Step 1) : sans le neutraliser, toute conversion écran → SVG divise par zéro et les tests de gestes mentent.
- `SEED_VERSION` : non incrémenté, rien n'apparaît et l'on croit l'implémentation cassée.
- Les schémas ne touchent jamais la file de synchronisation : aucun `enqueuePut` dans le CRUD.
- La convention du projet 7 : garder (`guard`) d'abord, muter l'état local ensuite.
